import { FlatList, Text, View } from "react-native";

const UserFavoritesCard = ({}) => {
  // pass or use context here idk yet
  return (
    <View>
      <View></View>
    </View>
  );
};

export default UserFavoritesCard;
